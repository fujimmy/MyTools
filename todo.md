# TODO

> 未完成事項統一記錄於此。

## Unfinished

### P1 高優先（高頻、低成本）

- [ ] 新增 URL Encoder / Decoder 工具（含 `encodeURIComponent` / `decodeURIComponent`）
- [ ] 新增 Unix Timestamp Converter（秒、毫秒、ISO、本地時間互轉）
- [ ] 新增 UUID / ULID Generator（支援批次產生）
- [ ] 新增 Hash / HMAC 工具（MD5、SHA-1、SHA-256）
- [ ] 新增 Regex Tester（flags、capture groups、replace 預覽）
- [ ] 新增 Diff Compare（兩段文字差異比對）

### P2 次優先（進階需求）

- [ ] 新增 JSON Schema Validator（顯示錯誤路徑）
- [ ] 新增 Cron Expression Helper（人類可讀與下次觸發時間）
- [ ] 新增 SQL Formatter（格式化與壓縮）
- [ ] 強化 JWT Decoder：加入簽章與 claim 驗證（exp、nbf、aud、iss）

## Done

- [x] 新增 SQL Practice 工具（家具訂單假資料、Schema 設定、JSON 匯入、前端 SQL 練習）
